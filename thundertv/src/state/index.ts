// Reserved for the Spektrum store modules (state/playlist, state/player,
// state/epg, state/settings, state/ui), the defineFn action layer, and the
// persistence bridge. Kept out of src/ui/ — state and rendering stay
// separate modules even though Spektrum binds them together at runtime.
// Owner: Phase 05 — Spektrum State Architecture.
export {};
